// firebase.js
import firebase from '@react-native-firebase/app';
import '@react-native-firebase/auth';

// Optionally, you can export the firebase instance if you need to use it elsewhere
export { firebase };
